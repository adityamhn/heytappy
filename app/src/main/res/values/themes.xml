<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.Tappy" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- Invisible trampoline used by AssistActivity: no window, no animation. -->
    <style name="Theme.Tappy.Transparent" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>
</resources>
