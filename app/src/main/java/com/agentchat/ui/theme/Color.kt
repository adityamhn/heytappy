package com.agentchat.ui.theme

import androidx.compose.ui.graphics.Color

// Tappy palette — energetic & warm, built around the logo's indigo-purple on cream.

// Brand purple (sent bubbles, buttons, accents). Logo core is #4F41D1; we nudge
// a touch brighter so it pops against the cream canvas.
val BrandPurpleLight = Color(0xFF5A47E6)
val BrandPurpleDark = Color(0xFF8B7BFF)

// Received bubble — soft lavender instead of flat gray, keeps things lively.
val BubbleReceivedLight = Color(0xFFECE7FB)
val BubbleReceivedDark = Color(0xFF2A2740)
val OnBubbleReceivedLight = Color(0xFF211F2B)
val OnBubbleReceivedDark = Color(0xFFECEAF7)

// Chat canvas — warm cream (straight from the logo) / warm near-black.
val BackgroundLight = Color(0xFFFDF7F1)
val BackgroundDark = Color(0xFF141119)

// Elevated surfaces (input pill, banners, suggestion chips).
val SurfaceElevatedLight = Color(0xFFF2ECFB)
val SurfaceElevatedDark = Color(0xFF221E30)

// Secondary text (timestamps, system notices).
val SecondaryLabelLight = Color(0xFF716B80)
val SecondaryLabelDark = Color(0xFFA49EB6)

// Hairline dividers — warm on cream, cool in the dark.
val DividerLight = Color(0xFFEBE1D7)
val DividerDark = Color(0xFF2E2A3B)

// Success / connected state.
val AgentGreen = Color(0xFF2FBF71)

// Error — warm red.
val ErrorRed = Color(0xFFFF473A)

// Playful accents (from the brand collage) for suggestion chips & highlights.
val AccentLime = Color(0xFF8FDB5B)
val AccentOrange = Color(0xFFFB7A3C)
val AccentPink = Color(0xFFF49CC4)
val AccentLavender = Color(0xFFB6A6F6)
